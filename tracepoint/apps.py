from django.apps import AppConfig


class TracepointConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "tracepoint"
    verbose_name = "TracePoint physical custody"
