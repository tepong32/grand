# Finance operational scrutiny - 2026-09-09

Status: **IN PROGRESS / NO-GO**. This is the evidence record for the [mandatory gate](FINANCE_OPERATIONAL_SCRUTINY.md), not a completed certification. Starting software baseline: development checkpoint v0.7.68 (`d809977`), with all 699 project tests passing in 292.911 seconds. Personal-handoff adapters are implemented for their [documented coverage](FINANCE_WORK_VIEW_COVERAGE.md); generic following, shared views and notifications remain deferred.

## Source refresh and access evidence

Reviewed on 2026-09-09. Retrieved documents, indexed official metadata and unresolved local applicability are distinguished below. Search silence does not prove that no later issuance exists. No source changes an active local tax rule, accepted form or financial authority automatically.

| Reference | Evidence obtained in this pass | GRAND implication / remaining confirmation |
| --- | --- | --- |
| [DBM LBC 152](https://www.dbm.gov.ph/index.php?catid=85&id=85%3Alocal-budget-circular-no-152&view=article) | Official page retrieved; links the August 2, 2023 circular and identifies the 2023 LGU budget manual. Circular PDF retrieved through its link. An indexed reprinted-manual URL returned HTTP 404. | Preserve the planning/budget linkage and controlled local form mapping; full manual/form currency and enabled-LGU applicability remain to verify. |
| [DBM current LBC index](https://www.dbm.gov.ph/index.php/local-budget-circulars) | Retrieved index includes 2026 circulars on personnel, PS limits, support funds and a barangay manual. | Review fiscal-year/fund/transaction scope. A barangay manual is not automatically municipal authority. |
| [COA 2021-007](https://www.coa.gov.ph/wpfd_file/coa-circular-no-2021-007-september-6-2021/) | Indexed official title identifies deferment of the LGU GAM under Circular 2020-004; direct page retrieval failed. No later controlling text was established by this pass. | Keep EV-OFF-005 unresolved. Accounting and the assigned COA auditor must confirm the controlling manual; do not substitute the national-agency GAM. |
| [COA 2016-004](https://www.coa.gov.ph/wpfd_file/coa-circular-no-2016-004-september-30-2016/) | Indexed official description identifies LGU chart conversion and financial-statement annexes. | Reconcile the accepted chart and statement mapping to the applicable amendment chain; no automatic master-data activation. |
| [COA 2023-004](https://www.coa.gov.ph/wpfd_file/coa-circular-no-2023-004-june-14-2023/) | Indexed official title identifies updated documentary requirements amending 2012-001. Direct COA and an official DENR PDF copy were inaccessible in this pass. | Transaction-specific checklist extraction and local exception/routing confirmation remain incomplete. |
| [RA 7160, Supreme Court E-Library](https://elibrary.judiciary.gov.ph/thebookshelf/showdocs/2/53542) | Official indexed Act identified; direct body request returned HTTP 502. | Obtain readable provisions and applicable amendments before completing the statutory process mapping. |
| [GPPB Circular 01-2025 / Resolution 05-2025](https://www.gppb.gov.ph/wp-content/uploads/2026/02/GPPB-Resolution-No.-05-2025_circ.pdf) | Six-page official PDF retrieved. It distinguishes ongoing procurement from the newer framework and records transition/form milestones. | External procurement evidence needs the applicable regime and publication/effectivity basis; a generic legacy checklist cannot establish that. Confirm the LGU's actual procurement scope and source documents. |
| [BIR 1601-EQ guidance](https://efps.bir.gov.ph/efps-war/EFPSWeb_war/forms2018Version/1601EQ/1601eq_guidelines.html) | Official indexed guidance found; direct eFPS request returned HTTP 502. | Current return/channel/ATC applicability remains a local confirmation, not a filing claim from a GRAND source schedule. |
| [BIR RMC 36-2026](https://bir-cdn.bir.gov.ph/BIR/pdf/RMC%20No.%2036-2026.pdf) | Official PDF downloaded and both pages visually checked: dated April 28, 2026; announces eBIRForms 7.9.6.0 and five-digit TIN branch codes. Brief source excerpt: “from three (3) digits to five (5) digits”. | Existing tax-ID fields have 40-character capacity. Verify exact report preservation of a synthetic five-digit branch identifier. New ATCs/rates in the circular are not activated or presumed applicable to an LGU. |

See the [official-source register](finance-discovery/OFFICIAL_SOURCE_REGISTER.md) for the earlier baseline and the required local intake. Access failures are evidence limitations, not a finding that a law or circular is invalid.

## Runtime and recovery preflight

The existing Docker Desktop installation was stopped; its Linux engine was started for this isolated engineering check. Engine reports 29.7.2, and no local images were initially listed. The official MySQL Community image was pinned to `sha256:7dcc4add9183664de3a214daf85a50c3ba6cccfd7534f700b6561bf5b41885be`; both disposable servers report 8.4.12. The actual intended production server/version, approved off-host destination and named witness remain unresolved.

The existing backup tests use synthetic SQL output and mocked native process boundaries. Their passing result does not prove a native restore. The production backup code explicitly refuses to label a SQLite copy as a native MySQL recovery point. A new rehearsal must use separate synthetic stores, native dumps, an independently retained manifest hash and an isolated restore destination; it must leave the user's database unchanged.

### Native engineering rehearsal obtained

- PASS: migrated separate synthetic default/Finance stores and executed `VoucherWorkflowTests.test_complete_supplier_disbursement_route_uses_one_shared_case` through its real services and assertions on MySQL. This fixture uses the legacy shadow Budget certification route; it does not demonstrate the authoritative annual-budget chain or actual procurement acceptance.
- PASS: `create_backup_set` used its actual native writer with a transport-only wrapper for the official container client. The database account had privileges only on the two synthetic schemas. No dump command flags or application backup code were changed. The source was idle during these initial dumps; subsequent concurrent-write evidence is recorded below.
- Backup ID: `20260909T122309369959Z-7bc03c26`; independently retained manifest SHA-256: `6fba88c16d76f1ac66a4388871b82ea9314d00a0d28d0ec33aae3a320a9f6bb5`. Default gzip SHA-256: `2dcb6d5912fe83db836a0934a123804978b826104784746bf75cf1144831bafe`; Finance gzip SHA-256: `f54b2a9d1fb1e0243f4257b911b455b4d8427afe227e96a29217ab904bec2b42`.
- PASS: copied the completed set to another local directory, verified it against the retained manifest hash, and restored with the native client into the second disposable server. SQL import took 3.344 seconds; this is not end-to-end RTO or an approved operational target. The manifest still says `restore_tested: false`; this separate report records the engineering result.
- PASS: sorted full-row SHA-256 comparisons matched all 159 default tables (1,420 rows) and 44 Finance tables (160 rows), including migration records. Both stores remained separate. Cross-store case `c235babd-1d08-44dd-b5e5-f3a89e7c5f1c` resolves to retained entry `b65dfd8d-9465-4b3b-9437-20e92e3dcfec`; posting/source checksum validation passed after restore.
- PASS: independent Decimal sums from restored rows reproduce recognition debit = credit = 1,000.00; gross 1,000.00 minus deduction 100.00 = net/released 900.00; the two released instruments are 400.00 and 500.00. Exactly one posting-sync and completion event remained, with office attribution. The explicit validator permission remained present; the outsider and unassigned platform superuser lacked it. These are sampled access checks, not the full adversarial role gate.
- Harness limitations/errors: Docker's internal network did not publish Windows loopback ports, causing a refused connection before migrations. Only the labelled empty rehearsal containers were recreated on a dedicated bridge with loopback-only ports. A rehearsal helper import and a test-settings initialization error were corrected; neither was an application failure. Local `.env` loading is disabled in the harness. No user database or real credentials were used.
- Remaining external gates: production-compatible off-host custody, named witness, approved RPO/RTO and secure disposal. Subsequent native regression, runtime relocation, report reproduction and concurrent-write findings are recorded below. Same-host container isolation does not satisfy off-host acceptance.

Execution harness and detailed logs are ignored under `.tmp/native_scrutiny.py`, `.tmp/finance-scrutiny-20260909/` and `.tmp/finance-scrutiny-native-*.log`; synthetic credentials/dumps are not committed. The receipt is evidence of this specific rehearsal, not a general backup certification.

## Execution and traceability

**Blocking finding: FIN-GAP-032 (CRITICAL - VERIFIED for the supported fixed single-server capture).** A subsequent real-service interleaving between the two native dumps produced a checksum-valid completed set whose restore had a posted 1,000.00 journal without its source voucher/request. See the [gap reproduction](FINANCE_GAP_REGISTER.md#fin-gap-032---separate-native-snapshots-can-lose-cross-store-source-evidence). The idle-source result above remains valid only for that executed idle capture. The reproduced defect is verified for the supported topology; overall scrutiny and production remain NO-GO.

### Coordinated capture remediation evidence

- New capture holds a dedicated global read lock across both native dumps, validates a common MySQL UUID and checks the locking connection identity before/after each dump and before release. Different-server topology, missing privilege or connection loss fails closed. Both stores/artifacts remain separate. New capture metadata is structurally validated; legacy manifests remain integrity-readable without being labelled coordinated. D-060 and [the backup contract](DATABASE_BACKUP.md#coordinated-capture-requirement) document privileges and the server-wide write pause.
- NEW PASS: the actual native attempt without capture privilege was rejected before publication. Only the disposable source user's `FLUSH_TABLES` privilege was then granted for the successful engineering test; no real deployment grant was made.
- NEW PASS: a real voucher recognition workflow was started after the first dump. MySQL reported `Waiting for global read lock`; it could not finish while the capture held its lock, and completed normally after release. The completed set `20260909T123816317292Z-6dbb2977` has retained manifest SHA-256 `fb187da5807ec6bee718abcacbe832ad14ddaffe2a1f00ab8d93230758534d11`. Lock receipt: 20:38:16.413926 through 20:38:19.242748, UTC+08:00.
- NEW PASS: native restore exactly matched all 203 pre-capture table controls (default 1,446 rows; Finance 169). Both existing case/request/JEV chains resolve and pass immutable source/posting checks; independent debit = credit = 2,000.00; orphan count zero. The case completed after release is absent from both restored stores, rather than being captured in Finance alone.
- NEW PASS: killed the dedicated native lock connection after the first dump. The generator published no set, retained previous completed manifests, cleared staging and its filesystem lock, and subsequently completed a clean native retry. Retry ID `20260909T124032409164Z-c8975b65`, retained manifest SHA-256 `01734c14de953df302e4d08e5362fc2bca6648e6b5258f781d4c6d4500fd6a40`.
- NEW PASS: relocated the synthetic media tree and read all eight restored FileField references with matching bytes (one Finance template plus seven references to the shared default avatar). The first attempt exposed a harness omission: the source fixture lacked the repository-tracked default avatar. Adding that tracked baseline asset to the synthetic source and copied tree resolved it. This is same-host runtime-file engineering evidence, not off-host custody or a complete production media inventory.
- Focused backup tests: 26 passed in 0.460 seconds before a test-assertion placement cleanup and stricter UUID syntax check. Subsequent schema changes and final regression results are recorded below. The pre-fix native full-suite attempt was deliberately stopped after the critical discovery so validation could run on the repair; it has no full-pass result and must not be cited as one.
- Intermediate SQLite regression before the schema repairs: PASS, `.venv/Scripts/python.exe manage.py test --noinput`, 706 tests in 312.806 seconds. System, migration-drift and compilation checks passed. The corresponding native run failed as recorded below; it is not the final checkpoint result.

### Native schema warnings requiring follow-up

**First native full-suite outcome: FAIL - PRE-EXISTING, repaired in this checkpoint.** The first 706-test native run completed in 855.461 seconds with two failures and two errors. Three affected tests encounter the authoritative Budget/payable source-kind capacity defect (FIN-GAP-033); the fourth demonstrates missing original-request uniqueness (FIN-GAP-034). Both causes are present at `d809977`, before this checkpoint. The failures are preserved in `.tmp/finance-scrutiny-native-v069-first-full.log`. They are not dismissed as unrelated: the schema controls were repaired and rerun before checkpoint verification.

The source-kind field now accommodates its unchanged 27/40-character values. Budget's original-reference, nonblank-number and active-allocation invariants use generated nullable keys plus ordinary unique constraints. Each migration checks for historical duplicates before schema changes and refuses automatic cleanup. The first four-case rerun passed three tests and exposed the active-allocation constraint at the next assertion (28.832 seconds). After that extension, all five native reproduction/identity cases passed in 30.021 seconds, including the authoritative full-chain replay. The final 178-test affected native regression passed in 182.916 seconds. Full final-code and recovery results are recorded below. D-061 records the decision. No live/user database migration was performed.

MySQL reports unsupported conditional unique constraints in several installed applications, including Finance source versions, period-close policy, bank matching/outstanding items and reporting controls. Record these as absent database enforcement, then check each real service's serialization and invariant handling; a sequential full-suite result alone does not establish concurrency safety. This is a follow-up audit, not a demonstrated financial-corruption finding for every listed model.

An initial period-close-policy experiment inserted a synchronization barrier after each service's normal validation. Both transactions rolled back on barrier timeout and no active policy remained; it did **not** reproduce duplicate activation and is not a passing approval/concurrency test. The scheduling harness needs to respect existing row-lock serialization before drawing a conclusion. No policy runtime code or schema was changed in this checkpoint.

A subsequent ordinary simultaneous-start test used the unchanged service without an internal validation barrier. Both independent decisions completed in serialized order; the later decision superseded the earlier activation, leaving exactly one active policy. This one invariant check passed (`.tmp/finance-scrutiny-native-policy-parallel.log`); it does not prove every missing conditional constraint is compensated or establish intended activation ordering for all successor scenarios.

The following is a partial evidence matrix. New execution and earlier software coverage are distinguished; an earlier test pass is not new operational observation.

| Process / responsible office | Module and evidence | Approval / ledger / Treasury / report impact | Audit and execution status |
| --- | --- | --- | --- |
| Planning and external procurement / requesting office and external owners | Budget proposal/PAP links and payable document intake; actual planning/inspection packet not supplied | External authorization, applicable procurement regime and receipt evidence must precede accepted payable processing | UNRESOLVED local evidence. Source refresh above does not complete statutory or local process mapping. |
| Annual budget, allotment, obligation / Budget and independent authorizer | Authoritative replay test `VoucherWorkflowTests.test_authoritative_budget_to_reconciled_treasury_report_replay` | Links accepted setup/opening controls, appropriation/allotment/obligation, payable and downstream bank/report reconciliation | PRIOR SOFTWARE EVIDENCE: included in the passing v0.7.68 699-test suite. NEW PASS: authoritative native service replay, generated-key restore and report reproduction; details below. |
| Shadow supplier route / Budget, Accounting, Treasury | Native service replay `test_complete_supplier_disbursement_route_uses_one_shared_case` | Distinct preparer/validator; 1,000.00 gross and recognition, 100.00 deduction, two releases totalling 900.00; reviewed/acknowledged advice | NEW PASS on MySQL 8.4.12. Posting and completion audit events and attributed offices survive the idle-source restore. This route uses synthetic shadow Budget certification. |
| Native posting lineage / Accounting | Restored voucher/request/entry UUIDs and immutable payload/rule checksums | Recognition debit/credit 1,000.00; preserved payable and withholding detail; independent posting actor | NEW PASS for idle-source recovery; full-row hashes matched both stores. Subsequent coordinated-capture evidence resolves the reproduced race within the supported topology. |
| Tax-source reporting / Accounting | `reporting.test_finance_reporting`; generated XLSX read with openpyxl | Exact synthetic five-digit TIN branch string preserved through governed tax detail/report and reversal assertions | NEW PASS: all 29 tests, 8.924 seconds, SQLite test stores. No active tax rate/ATC or official filing change. |
| Correction, return, reversal, retry and close / owning offices and independent reviewers | Existing Accounting, Budget, voucher and cutover suites | Preserve originals, linked corrections, zero-difference reconciliations and period barriers | PRIOR SOFTWARE EVIDENCE: v0.7.68 full suite. Final native regression passed; actual redacted replay and named-office acceptance remain outstanding. |
| Recovery under concurrent writes / IT with Finance control owners | Real service recognition inserted between actual native dumps; checksum verification and native restore | Restored 1,000.00 posted journal lacks its voucher and request | REPRODUCED then VERIFIED: FIN-GAP-032. Coordinated capture, blocked writer, native restore and killed-session/retry evidence are recorded above. |

Pending: complete the matrix for all required process/persona/adversarial paths and investigate the remaining native conditional-constraint/service-concurrency warnings. No claim of a completed full scrutiny is made.

The real LGU interviews, enabled scope, accepted forms, master data, opening balances, redacted examples, named roles, printer/device observations and witnessed production-compatible recovery remain the external gates in [CONTINUE.md](../CONTINUE.md).

### Next bank-concurrency finding

FIN-GAP-035 was open at v0.7.69 and is now verified in v0.7.70; see [the bank-conflict validation](FINANCE_BANK_MATCH_VALIDATION_2026-09-09.md). A native two-statement matching probe retained one active match but aborted the other transaction with a journal-line lock-upgrade deadlock. Repeating the interleaving through the actual authenticated endpoint returned HTTP 500 and HTTP 302 (1 failed regression, 6.792 seconds). The single-match count is an integrity observation for this experiment, not a passing user workflow or proof of all native uniqueness invariants. The gap register records the lock trace, safe interim check and bounded remediation scope. The bank-conflict repair is a separate v0.7.70 checkpoint; overall scrutiny remains incomplete.


## Final v0.7.69 checkpoint

Coordinated native capture prevents the reproduced cross-store snapshot race and fails closed on missing privilege, different servers or lost locking connections. Payable projections preserve complete source kinds and consolidated UUID references. Budget generated nullable keys enforce original requests, numbered obligations and active allocations on MySQL and SQLite. Duplicate-data migration preflights require controlled investigation instead of altering history. Migrations: vouchers/0022-0023 and budget/0007-0008; no live/user database migration.

PASS: final full SQLite suite, 708 tests in 314.043 seconds; final full MySQL 8.4.12 suite, 708 tests in 854.600 seconds. Both aliases remained separate. System, migration-drift, compilation and diff checks passed. The final affected native suite also passed 178 tests in 182.916 seconds.

The first native full run failed two tests and errored twice (706 tests, 855.461 seconds); the defects existed at d809977 and were repaired here. An additional four-obligation native probe exposed the 160-character reference limit (1 error, 3.209 seconds). The new retained four-source regression covers the text-field repair. Earlier SQLite passes (706 and 707 tests) are intermediate evidence.

An intermediate 708-test native run using retained test schemas failed with four errors and one failure in 741.387 seconds: the site singleton, public shortcuts and default leave policy were absent after a preceding TransactionTestCase flush, while their data migrations remained applied. FAIL - CAUSED BY CURRENT WORK (the test harness reused flushed fixtures; no new application regression), retained in `.tmp/finance-scrutiny-native-reused-fixture-failure.log`. The final native run recreates only the two named disposable test schemas and reapplies all migrations; this final fresh result supersedes that run.

Final commands: `.venv/Scripts/python.exe manage.py test --noinput` and `GRAND_SCRUTINY_FRESH_TESTS=1 .venv/Scripts/python.exe .tmp/native_scrutiny.py test source` (set the environment variable with PowerShell syntax on Windows). The ignored native harness disables local .env loading and uses only explicit disposable test schemas. Native conditional-constraint warnings outside the repaired Budget invariants remain follow-up work; a sequential suite does not prove every concurrency invariant. A committed native CI gate is a next-phase improvement; no remote CI pass is claimed.

Final authoritative recovery receipt: manifest SHA-256 `97cdc121af92c58d558be55a4203d722143f18f85a80d90ab6c896ff1219cfbb`. Applied the final projection migration only to the owned synthetic source. Both native stores restored with all 203 table hashes equal; Budget generated keys and all ten stored file references survived. With original runtime trees temporarily unavailable, restored evidence reproduced the payment report (issued/released 900.00), posted debit = credit = 3,900.00, and bank balance 1,100.00 with zero difference. The original runtime trees were restored after the check. This is same-host engineering evidence, not witnessed off-host acceptance.

Browser NOT RUN - OUT OF SCOPE: unchanged application templates; changes concern persistence and backup CLI. Actual LGU discovery, forms, opening balances, redacted replay, named-user/printer checks, approved maintenance window, production-compatible off-host restore and seven-party acceptance remain unperformed. Production remains NO-GO.
